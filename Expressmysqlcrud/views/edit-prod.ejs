<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update</title>
    <style>
        /* General form styling */
        form {
            width: 400px;
            /* Adjust as needed */
            margin: 0 auto;
            /* Center the form */
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        fieldset {
            border: none;
            padding: 10px 0;
        }

        legend {
            font-size: 1.2rem;
            font-weight: bold;
            margin-bottom: 10px;
        }

        /* Field labels */
        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }

        /* Input fields */
        input[type="text"],
        input[type="number"] {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 3px;
            margin-bottom: 10px;
        }

        /* Readonly field styling */
        input[readonly] {
            background-color: #eee;
            cursor: not-allowed;
        }

        /* Button styling */
        button[type="submit"] {
            background-color: #4CAF50;
            /* Green */
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 3px;
            cursor: pointer;
        }
    </style>
</head>

<body>
    <form action="/updateproduct" method="post">
        <fieldset>
            <legend>Update Product</legend>
            <div>
                <label for="pid">Product ID:</label>
                <input type="number" name="pid" id="pid" value="<%= prod.pid %>" readonly>
            </div>
            <div>
                <label for="pname">Product Name:</label>
                <input type="text" name="pname" id="pname" value="<%= prod.pname %>" required>
            </div>
            <div>
                <label for="qty">Product Quantity:</label>
                <input type="number" name="qty" id="qty" value="<%= prod.qty %>" min="1" required>
            </div>
            <div>
                <label for="price">Product Price:</label>
                <input type="number" name="price" id="price" value="<%= prod.price %>" step="0.01" required>
            </div>
            <button type="submit">Update Product</button>
        </fieldset>
    </form>

</body>

</html>